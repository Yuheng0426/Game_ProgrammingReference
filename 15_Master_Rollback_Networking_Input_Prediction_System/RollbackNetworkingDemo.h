#pragma once

// Lesson entry point for rollback networking and client-side input prediction.
//
// This lesson follows deterministic replay because rollback depends on replaying inputs.

namespace game_ref
{
    void RunRollbackNetworkingDemo();
}
