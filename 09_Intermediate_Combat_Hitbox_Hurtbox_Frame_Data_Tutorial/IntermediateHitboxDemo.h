#pragma once

// Lesson entry point for active frames, hitboxes, hurtboxes, and frame data.
//
// This lesson is important for melee combat because damage should happen during
// intentional attack windows, not during every frame of an animation.

namespace game_ref
{
    void RunIntermediateHitboxDemo();
}
