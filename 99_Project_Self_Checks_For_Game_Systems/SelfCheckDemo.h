#pragma once

// Lesson entry point for lightweight self checks.
//
// These checks are not a full testing framework, but they teach the habit of
// verifying core gameplay assumptions after changing shared systems.

namespace game_ref
{
    void RunSelfCheckDemo();
}
