#pragma once

// Lesson entry point for save/load, player profile data, checkpoints, and settings.
//
// The demo uses a small text format so learners can read serialized data directly.

namespace game_ref
{
    void RunSaveLoadDemo();
}
